#include "block.h"

void block_init(void) {
    BLOCK_DECL(dirt);
    BLOCK_DECL(grass);
    BLOCK_DECL(air);
    BLOCK_DECL(stone);
    BLOCK_DECL(sand);
    BLOCK_DECL(water);
    BLOCK_DECL(bedrock);
}
